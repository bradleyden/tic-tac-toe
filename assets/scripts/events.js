'use strict'

// const engine = require('./engine')

const onClick = function () {
  console.log('test')
  // if (engine.isPlayerOne) {
  //   engine.placeX(space)
  // } else {
  //   engine.placeO(space)
  // }
}

module.exports = {
  onClick
}
