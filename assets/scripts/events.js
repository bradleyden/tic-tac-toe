'use strict'

const engine = require('./engine')

const onClick = function (space) {
  console.log('test')
  // if (engine.isPlayerOne) {
  //   engine.placeX(space)
  // } else {
  //   engine.placeY(space)
  // }
}

module.export = onClick
